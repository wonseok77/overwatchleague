## Batch 4 Context (2026-03-06)

### Critical DB Issue
- heroes table in migration 000: columns (id, name, role, image_url, key)
- Hero model expects: (id, name, role, portrait_url, is_custom, created_at)
- MISMATCH: image_url vs portrait_url, missing is_custom/created_at
- Need migration 005 to fix this

### Issues to Fix
1. Hero add fails — DB schema mismatch (see above)
2. Profile: show season-specific position tiers (currently only global ranks)
3. MatchListPage: calendar with season periods colored, sessions grouped by season
4. SessionDetailPage: verify edit dialog + matchmaking work after latest rebuild

### Codebase Patterns
- FastAPI + SQLAlchemy 2.0 + PostgreSQL backend
- React 18 + Vite + TypeScript + Tailwind frontend
- Docker Compose: frontend:3000 + backend:8000 + PostgreSQL:5432
- Alembic migrations: 000 → 001 → 002 → 003 → 004
- Custom Dialog (NOT shadcn): open prop, onClose callback, fixed overlay z-50
- Auth: JWT, role-based (admin/manager/member)
- 장원석 = admin user (real_name)
- Tier-MMR: Champion=4500, GM=4000, Master=3500, Diamond=3000, Plat=2500, Gold=2000, Silver=1500, Bronze=1000
- Python 3.9: use Optional[str] not str | None
- hero_role enum in DB (from migration 000) — different from player_role enum

### File Boundaries
- Backend agent: backend/ only
- Frontend agent: frontend/src/ only
- Shared types: frontend/src/types/index.ts
